# Backdrop

The Backdrop is the grid of node viewers that are visible behind the [Network](<./Network.md> "Network"), set by turning on [Display Flags](<./Display_Flag.md> "Display Flag") and the network RMB -> Display... Backdrop OPs.
