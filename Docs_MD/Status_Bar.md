# Status Bar

The status bar is at the top of the TouchDesigner interface, to the right of the menus. 

The status bar will display messages from TouchDesigner when certain events succeed or fail. 

You can display any message in the status bar by using the _status_ command.
