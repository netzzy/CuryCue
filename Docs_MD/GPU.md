# GPU

The Graphics Processing Unit. This is the brains of the graphics card, and consists of 4 to hundreds of processors.   
  
It's where TOPs are computed. It also runs [CUDA](<./CUDA.md> "CUDA") programs.
