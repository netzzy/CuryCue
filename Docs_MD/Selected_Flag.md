# Selected Flag

Every component contains one current node, and one or more selected nodes. Clicking a node makes it selected and current. Box selecting nodes selects multiple nodes at once. The current node has its Current flag set, while selected nodes have their Selected flags set.   
  
See also [Current Flag](<./Current_Flag.md> "Current Flag"), [Flag](<./Flag.md> "Flag").
